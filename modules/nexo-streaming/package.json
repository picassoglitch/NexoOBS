{
  "name": "nexo-streaming",
  "version": "0.1.0",
  "description": "RTMP broadcaster for NexoStreamOBS — Android via RootEncoder, iOS via HaishinKit (later). Local Expo Module; only loads in a custom dev client.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "private": true
}
